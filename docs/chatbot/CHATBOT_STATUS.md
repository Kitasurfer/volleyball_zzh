# 🤖 Статус чатбота

## Проблема

`ingest-content` функция не запускается (BOOT_ERROR). Это означает, что есть проблема при инициализации функции.

## Возможные причины:

1. **Qdrant коллекция не создана** - функция пытается подключиться к несуществующей коллекции
2. **OpenAI ключ неправильный** - проверь что ключ начинается с `sk-proj-`
3. **Проблема с импортами** - возможно конфликт между файлами

## Что делать:

### Вариант 1: Создай Qdrant коллекцию (рекомендую)

1. Зайди на https://cloud.qdrant.io/clusters
2. Collections → Create Collection
3. Name: `content_vectors`
4. Vector size: `1536`
5. Distance: `Cosine`
6. Payload indexes: добавь `language` (keyword)
7. Create

После этого попробуй снова запустить индексацию.

### Вариант 2: Проверь OpenAI ключ

Зайди в Supabase Secrets и убедись что:
- `OPENAI_API_KEY` начинается с `sk-proj-` или `sk-`
- Ключ активен на https://platform.openai.com/api-keys

---

## Текущий статус:

- ✅ **League-results v21** - работает
- ✅ **Chatbot v17** - задеплоен с OpenAI
- ❌ **Ingest-content v20** - BOOT_ERROR

---

Скажи мне когда создашь коллекцию в Qdrant, я сразу запущу индексацию!
