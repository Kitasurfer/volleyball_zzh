# 🔄 Git Workflow — Правильная работа с репозиторием

## 📋 Содержание

1. [Базовые команды](#базовые-команды)
2. [Ежедневный workflow](#ежедневный-workflow)
3. [Работа с ветками](#работа-с-ветками)
4. [Решение проблем](#решение-проблем)
5. [Best Practices](#best-practices)

---

## 🚀 Базовые команды

### Проверка статуса

```bash
# Показать текущий статус (измененные файлы)
git status

# Показать историю коммитов
git log --oneline -10

# Показать удаленные репозитории
git remote -v
```

### Добавление изменений

```bash
# Добавить все измененные файлы
git add .

# Добавить конкретный файл
git add src/pages/ContactPage.tsx

# Добавить все файлы определенного типа
git add *.tsx

# Добавить файлы из конкретной папки
git add src/components/
```

### Коммит изменений

```bash
# Создать коммит с сообщением
git commit -m "Fix: Update contact email"

# Создать коммит с подробным описанием
git commit -m "Feature: Add news page" -m "- Added news listing
- Added news detail page
- Integrated with Supabase content API"

# Изменить последний коммит (до push)
git commit --amend -m "Fix: Update contact email to correct address"
```

### Отправка в репозиторий

```bash
# Отправить изменения в удаленный репозиторий
git push

# Отправить в конкретную ветку
git push origin main

# Принудительная отправка (ОСТОРОЖНО!)
git push --force
```

---

## 📅 Ежедневный workflow

### Начало работы

```bash
# 1. Перейти в директорию проекта
cd /Users/bogdanfesenko/StudioProjects/Volleyball/volleyball_zzh

# 2. Проверить статус
git status

# 3. Получить последние изменения
git pull

# 4. Проверить текущую ветку
git branch
```

### Внесение изменений

```bash
# 1. Внести изменения в файлы (через IDE или редактор)

# 2. Проверить что изменилось
git status
git diff

# 3. Добавить файлы в staging
git add .

# 4. Создать коммит
git commit -m "Feature: Add tournament results page"

# 5. Отправить в репозиторий
git push
```

### Завершение работы

```bash
# 1. Убедиться что все закоммичено
git status

# 2. Проверить что все отправлено
git log origin/main..HEAD
# Если вывод пустой — всё отправлено

# 3. Если есть неотправленные коммиты
git push
```

---

## 🌿 Работа с ветками

### Создание новой ветки

```bash
# Создать и переключиться на новую ветку
git checkout -b feature/news-page

# Или в два шага
git branch feature/news-page
git checkout feature/news-page
```

### Переключение между ветками

```bash
# Переключиться на main
git checkout main

# Переключиться на другую ветку
git checkout feature/news-page

# Посмотреть все ветки
git branch -a
```

### Слияние веток

```bash
# 1. Переключиться на main
git checkout main

# 2. Получить последние изменения
git pull

# 3. Слить ветку feature
git merge feature/news-page

# 4. Отправить изменения
git push

# 5. Удалить ветку (опционально)
git branch -d feature/news-page
```

---

## 🔧 Решение проблем

### Проблема: Permission denied (deploy key)

**Ошибка**:
```
ERROR: Permission to Kitasurfer/volleyball_zzh.git denied to deploy key
fatal: Could not read from remote repository.
```

**Решение**:
```bash
# Проверить SSH конфигурацию
cat ~/.ssh/config

# Изменить remote URL на правильный
git remote set-url origin git@github.com-volleyball:Kitasurfer/volleyball_zzh.git

# Проверить что изменилось
git remote -v

# Попробовать снова
git push
```

### Проблема: Конфликты при pull

**Ошибка**:
```
CONFLICT (content): Merge conflict in src/pages/HomePage.tsx
```

**Решение**:
```bash
# 1. Открыть файл с конфликтом
# Найти маркеры конфликта:
# <<<<<<< HEAD
# ваши изменения
# =======
# изменения из репозитория
# >>>>>>> origin/main

# 2. Вручную разрешить конфликт (удалить маркеры, выбрать нужный код)

# 3. Добавить разрешенный файл
git add src/pages/HomePage.tsx

# 4. Завершить merge
git commit -m "Merge: Resolve conflicts in HomePage"

# 5. Отправить
git push
```

### Проблема: Нужно отменить последний коммит

```bash
# Отменить последний коммит, но сохранить изменения
git reset --soft HEAD~1

# Отменить последний коммит и удалить изменения (ОСТОРОЖНО!)
git reset --hard HEAD~1

# Отменить последний коммит и создать новый
git revert HEAD
```

### Проблема: Случайно закоммитил не те файлы

```bash
# Убрать файл из последнего коммита
git reset HEAD~1 -- path/to/file.txt
git commit --amend --no-edit

# Или полностью переделать коммит
git reset --soft HEAD~1
git reset path/to/unwanted-file.txt
git commit -m "Fix: Correct commit without unwanted file"
```

### Проблема: Забыл pull перед push

**Ошибка**:
```
! [rejected]        main -> main (non-fast-forward)
```

**Решение**:
```bash
# Вариант 1: Pull с rebase (рекомендуется)
git pull --rebase
git push

# Вариант 2: Pull с merge
git pull
git push

# Вариант 3: Если уверены в своих изменениях (ОСТОРОЖНО!)
git push --force
```

---

## ✅ Best Practices

### Сообщения коммитов

**Хорошие примеры**:
```bash
git commit -m "Fix: Update contact email address"
git commit -m "Feature: Add news listing page"
git commit -m "Refactor: Simplify standings hook logic"
git commit -m "Docs: Update admin guide with media instructions"
git commit -m "Style: Fix responsive layout on mobile"
```

**Плохие примеры**:
```bash
git commit -m "fix"
git commit -m "update"
git commit -m "changes"
git commit -m "asdfasdf"
```

### Префиксы коммитов

- **Fix**: Исправление бага
- **Feature**: Новая функциональность
- **Refactor**: Рефакторинг кода
- **Docs**: Обновление документации
- **Style**: Изменения стилей/форматирования
- **Test**: Добавление тестов
- **Chore**: Рутинные задачи (обновление зависимостей и т.д.)

### Частота коммитов

✅ **Правильно**:
- Коммитить логически завершенные изменения
- Один коммит = одна задача/фича
- Коммитить несколько раз в день

❌ **Неправильно**:
- Коммитить всё в один большой коммит в конце дня
- Коммитить незавершенный/нерабочий код
- Коммитить после каждого изменения (слишком часто)

### Что НЕ коммитить

❌ Не добавляйте в Git:
- `node_modules/`
- `.env` файлы с секретами
- `dist/` или `build/` папки
- IDE конфигурации (`.vscode/`, `.idea/`)
- Временные файлы (`.DS_Store`, `*.log`)
- Большие бинарные файлы (используйте Git LFS)

✅ Эти файлы уже в `.gitignore`

---

## 🔄 Полный пример workflow

### Сценарий: Добавление новой страницы с новостями

```bash
# 1. Начало работы
cd /Users/bogdanfesenko/StudioProjects/Volleyball/volleyball_zzh
git status
git pull

# 2. Создать ветку (опционально)
git checkout -b feature/news-page

# 3. Внести изменения
# - Создать src/pages/NewsPage.tsx
# - Обновить src/App.tsx (добавить роут)
# - Обновить src/components/Navbar.tsx (добавить ссылку)

# 4. Проверить изменения
git status
git diff

# 5. Добавить файлы
git add src/pages/NewsPage.tsx
git add src/App.tsx
git add src/components/Navbar.tsx

# 6. Создать коммит
git commit -m "Feature: Add news listing page

- Created NewsPage component
- Added /news route
- Updated navigation menu
- Integrated with Supabase content API"

# 7. Отправить в репозиторий
git push

# Если это новая ветка, Git подскажет команду:
git push --set-upstream origin feature/news-page

# 8. Слить с main (если работали в ветке)
git checkout main
git pull
git merge feature/news-page
git push

# 9. Удалить ветку
git branch -d feature/news-page
```

---

## 📝 Шпаргалка команд

```bash
# Статус и информация
git status                    # Показать статус
git log --oneline -10        # История коммитов
git diff                     # Показать изменения
git branch                   # Список веток

# Работа с изменениями
git add .                    # Добавить все файлы
git add <file>               # Добавить конкретный файл
git commit -m "message"      # Создать коммит
git push                     # Отправить в репозиторий
git pull                     # Получить изменения

# Ветки
git checkout -b <branch>     # Создать и переключиться
git checkout <branch>        # Переключиться на ветку
git merge <branch>           # Слить ветку
git branch -d <branch>       # Удалить ветку

# Отмена изменений
git reset --soft HEAD~1      # Отменить последний коммит
git reset --hard HEAD~1      # Отменить коммит и изменения
git checkout -- <file>       # Отменить изменения в файле
git clean -fd                # Удалить неотслеживаемые файлы

# Удаленный репозиторий
git remote -v                # Показать remote
git remote set-url origin <url>  # Изменить remote URL
git fetch                    # Получить изменения без merge
git pull --rebase            # Pull с rebase
```

---

## 🎯 Быстрый старт для новичков

### Каждый раз когда работаете с кодом:

```bash
# 1. Открыть терминал
cd /Users/bogdanfesenko/StudioProjects/Volleyball/volleyball_zzh

# 2. Получить последние изменения
git pull

# 3. Внести изменения в коде

# 4. Сохранить изменения
git add .
git commit -m "Описание изменений"
git push
```

### Если что-то пошло не так:

```bash
# Отменить все локальные изменения
git reset --hard HEAD
git clean -fd

# Получить свежую копию из репозитория
git pull
```

---

## 📞 Помощь

### Полезные ссылки:
- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Oh Shit, Git!?!](https://ohshitgit.com/) — решение частых проблем

### Команды помощи:
```bash
git help <command>           # Справка по команде
git <command> --help         # То же самое
```

---

**Последнее обновление**: 9 ноября 2024
