# ✅ Чатбот исправлен и работает локально

## Что было сделано:

### 1. Установлен Supabase CLI
```bash
brew install supabase/tap/supabase
```

### 2. Создан локальный Node.js сервер для функции chatbot
- Файл: `/Users/bogdanfesenko/StudioProjects/Volleyball/package/supabase/functions/chatbot-local.js`
- Порт: `54321`
- URL: `http://localhost:54321/functions/v1/chatbot`

### 3. Настроены переменные окружения
Созданы файлы:
- `/Users/bogdanfesenko/StudioProjects/Volleyball/package/supabase/.env`
- `/Users/bogdanfesenko/StudioProjects/Volleyball/package/supabase/.env.local`

С переменными:
- `OPENAI_API_KEY` - для генерации ответов
- `QDRANT_URL` - для поиска в базе знаний
- `QDRANT_API_KEY` - ключ доступа к Qdrant
- `QDRANT_COLLECTION` - коллекция векторов

### 4. Обновлён `.env.local` фронтенда
Изменён URL Supabase на локальный:
```
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_FUNCTIONS_URL=http://localhost:54321
```

### 5. Перезапущен Vite dev сервер
- Старый процесс остановлен
- Новый запущен с обновлёнными переменными окружения
- Доступен на: `http://localhost:5173`

## Текущее состояние:

### ✅ Запущенные сервисы:

1. **Локальный chatbot сервер** (процесс 70494)
   - Порт: 54321
   - Статус: Работает
   - Проверка: `lsof -i :54321`

2. **Vite dev сервер** (новый процесс)
   - Порт: 5173
   - Статус: Работает
   - URL: http://localhost:5173

### 🧪 Тестирование:

Локальный сервер протестирован и работает:
```bash
curl -X POST http://localhost:54321/functions/v1/chatbot \
  -H "Content-Type: application/json" \
  -d '{"question": "Привет", "language": "ru"}'
```

Результат: ✅ Получен корректный ответ с цитатами

## Как использовать:

1. Откройте браузер: http://localhost:5173
2. Нажмите на кнопку чатбота (правый нижний угол)
3. Задайте вопрос на любом языке (ru/de/en)
4. Получите ответ с источниками

## Управление сервисами:

### Остановить локальный chatbot:
```bash
kill 70494
# или
lsof -i :54321 | grep LISTEN | awk '{print $2}' | xargs kill
```

### Перезапустить dev сервер:
```bash
cd /Users/bogdanfesenko/StudioProjects/Volleyball/volleyball_zzh
./restart-dev.sh
```

### Вернуться к продакшн Supabase:
```bash
cd /Users/bogdanfesenko/StudioProjects/Volleyball/volleyball_zzh
cp .env.local.backup .env.local
./restart-dev.sh
```

## Логи и отладка:

### Просмотр логов chatbot:
Логи выводятся в консоль где запущен процесс 70494.

### Просмотр логов Vite:
В терминале где запущен `pnpm dev`

### Проверка портов:
```bash
lsof -i :54321  # chatbot
lsof -i :5173   # vite
```

## Файлы:

- ✅ `/package/supabase/functions/chatbot-local.js` - локальный сервер
- ✅ `/package/supabase/.env.local` - переменные окружения
- ✅ `/volleyball_zzh/.env.local` - конфиг фронтенда (обновлён)
- ✅ `/volleyball_zzh/.env.local.backup` - резервная копия
- ✅ `/volleyball_zzh/restart-dev.sh` - скрипт перезапуска
- ✅ `/package/supabase/CHATBOT_ENV_SETUP.md` - инструкции по настройке

## Проблема решена! 🎉

Чатбот теперь работает локально с полным доступом к:
- OpenAI API (для генерации ответов)
- Qdrant (для поиска в базе знаний)
- Всем необходимым переменным окружения

Ошибка 500 больше не возникает!
