# 🤖 Чатбот - Краткая инструкция

## ✅ Проблема решена!

Чатбот теперь работает локально. Ошибка 500 исправлена.

## 🚀 Что запущено:

1. **Локальный chatbot сервер** на порту `54321`
2. **Vite dev сервер** на порту `5173` → http://localhost:5173

## 📝 Как пользоваться:

1. Откройте http://localhost:5173 в браузере
2. Нажмите на иконку чата (правый нижний угол)
3. Задайте вопрос
4. Получите ответ с источниками

## 🔧 Управление:

### Остановить chatbot:
```bash
lsof -i :54321 | grep LISTEN | awk '{print $2}' | xargs kill
```

### Перезапустить всё:
```bash
./restart-dev.sh
```

### Вернуться к продакшн серверу:
```bash
cp .env.local.backup .env.local
./restart-dev.sh
```

## 📄 Подробности:

Смотри `CHATBOT_FIXED.md` для полной информации.
